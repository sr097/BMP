{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "orval --config ./orval.config.ts && node -e \"const fs=require('fs');const p='../../lib/api-zod/src/index.ts';fs.writeFileSync(p,fs.readFileSync(p,'utf8').replace(/^export \\* from '.\\//gm,\\\"export type * from './\\\"))\" || true && pnpm -w run typecheck:libs"
  },
  "devDependencies": {
    "orval": "^8.18.0"
  }
}
